# VS-001 Evidence: 03 — PostgreSQL & Migrations

**Vertical Slice:** VS-001: Baseline & Local Runtime

**Date:** 2026-07-20

**Status:** PENDING REVIEW (database verification blocked by CI)

## Summary

This document outlines database setup, migration infrastructure, and data persistence. Actual verification requires local Docker execution.

## PostgreSQL Container Setup

**File:** `docker-compose.yml`

**Status:** ✅ CREATED

**Service Definition:**

```yaml
services:
  postgres:
    image: postgres:14-alpine
    container_name: powermech-postgres
    environment:
      POSTGRES_USER: powermech
      POSTGRES_PASSWORD: powermech_local
      POSTGRES_DB: powermech_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U powermech"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
```

**Key Features:**
- ✅ PostgreSQL 14 (Alpine Linux, minimal size)
- ✅ Named volumes for data persistence
- ✅ Health check for readiness verification
- ✅ Auto-restart on failure
- ✅ Standard port 5432

---

## Planned Startup Verification

**PLANNED COMMAND:**
```bash
docker-compose up -d postgres
sleep 5
docker-compose ps
```

**EXPECTED OUTPUT:**
```
NAME                COMMAND                  SERVICE    STATUS
powermech-postgres  "docker-entrypoint..."   postgres   Up 5s (healthy)
```

**STATUS:** ⏳ NOT EXECUTED

**Reason:** No Docker in CI environment.

---

**PLANNED VERIFICATION COMMAND:**
```bash
docker exec -it powermech-postgres psql -U powermech -d powermech_dev -c "SELECT version();"
```

**EXPECTED OUTPUT:**
```
PostgreSQL 14.9 (Debian 14.9-1.pgdg120+1) on x86_64-pc-linux-gnu
```

**STATUS:** ⏳ NOT EXECUTED

**Reason:** PostgreSQL container not running in CI.

---

## Migration Infrastructure

### Directory Structure

**File:** `migrations/.gitkeep`

**Status:** ✅ CREATED

```
migrations/
├── .gitkeep                    # Directory tracked in git
└── (future migration files)    # .sql files generated by node-pg-migrate
```

### Migration Tool: node-pg-migrate

**Package:** `node-pg-migrate@6.2.0`

**Status:** ✅ ADDED TO package.json

**Features:**
- Pure SQL migrations (no JavaScript)
- Automatic migration table (`pgmigrations`)
- Bidirectional (up/down)
- No ORM dependency
- CLI-based generation

---

## Migration Commands

**File:** `package.json` scripts

**Status:** ✅ CREATED

### 1. Apply Migrations

**PLANNED COMMAND:**
```bash
pnpm db:migrate
```

**EXPECTED OUTPUT (baseline, no migrations):**
```
Running migrations from migrations
✅ No pending migrations
```

**EXPECTED OUTPUT (after creating migration):**
```
Running migrations from migrations
✅ Migration 1721476800000_create_users_table.js applied
```

**STATUS:** ⏳ NOT EXECUTED

**Reason:** Database not running.

---

### 2. Undo Last Migration

**PLANNED COMMAND:**
```bash
pnpm db:migrate:down
```

**EXPECTED OUTPUT:**
```
Running migrations from migrations
✅ Migration 1721476800000_create_users_table.js reverted
```

**STATUS:** ⏳ NOT EXECUTED

**Reason:** No migrations to undo yet (baseline).

---

### 3. Reset Database

**PLANNED COMMAND:**
```bash
pnpm db:reset
```

**EXPECTED OUTPUT:**
```
Running migrations from migrations
Running migrations from migrations (down to initial)
✅ Reset complete
```

**STATUS:** ⏳ NOT EXECUTED

**Reason:** Database not running.

---

### 4. Create New Migration

**PLANNED COMMAND:**
```bash
pnpm db:migrate:create -- create_users_table
```

**EXPECTED OUTPUT:**
```
Created migration: migrations/1721476800000_create_users_table.js
```

**STATUS:** ⏳ NOT EXECUTED

**Reason:** Database not running.

---

## Migration File Pattern

**Example:** `migrations/1721476800000_create_users_table.js`

**Status:** NOT YET CREATED (baseline has no migrations)

**Pattern:**

```javascript
// Pure SQL migration (recommended for VS-001)
exports.up = async (pgm) => {
  pgm.createTable('users', {
    id: 'id',
    email: { type: 'varchar(255)', notNull: true, unique: true },
    created_at: { type: 'timestamp', default: pgm.func('current_timestamp') },
  });
};

exports.down = (pgm) => {
  pgm.dropTable('users', { ifExists: true });
};
```

**Note:** Baseline intentionally has NO business tables (users, vehicles, repairs, etc.).

---

## Data Persistence

### Volume Configuration

**File:** `docker-compose.yml`

**Status:** ✅ CREATED

```yaml
volumes:
  postgres_data:
    driver: local
```

**Persistence Behavior:**
- ✅ Data survives `docker-compose down`
- ✅ Data preserved across container restarts
- ✅ Data lost on `docker volume rm powermech-ai_postgres_data`

---

### Backup/Restore (Planned)

**PLANNED BACKUP COMMAND:**
```bash
docker exec powermech-postgres pg_dump -U powermech powermech_dev > backup.sql
```

**PLANNED RESTORE COMMAND:**
```bash
docker exec -i powermech-postgres psql -U powermech powermech_dev < backup.sql
```

**STATUS:** ⏳ NOT EXECUTED

**Reason:** Database not running.

---

## Migration State Management

### pgmigrations Table

**PLANNED VERIFICATION COMMAND:**
```bash
docker exec powermech-postgres psql -U powermech -d powermech_dev -c "SELECT * FROM pgmigrations;"
```

**EXPECTED OUTPUT (baseline, no migrations):**
```
no rows
```

**EXPECTED OUTPUT (after one migration):**
```
 id |                name                |       run_on
----+------------------------------------+-------------------
  1 | 1721476800000_create_users_table   | 2026-07-20 14:00:00
```

**STATUS:** ⏳ NOT EXECUTED

**Reason:** Database not running.

---

## Troubleshooting (Planned)

### Connection Refused

**PLANNED DIAGNOSIS:**
```bash
docker-compose logs postgres
```

**PLANNED RECOVERY:**
```bash
docker-compose down
docker-compose up -d postgres
sleep 5
docker-compose ps
```

**STATUS:** ⏳ NOT EXECUTED

**Reason:** No Docker in CI.

---

### Migration Lock

**PLANNED RECOVERY COMMAND:**
```bash
docker exec powermech-postgres psql -U powermech -d powermech_dev -c \
  "DELETE FROM pgmigrations WHERE name = '1721476800000_stuck_migration';"
```

**STATUS:** ⏳ NOT EXECUTED

**Reason:** Database not running.

---

## Database Go-Live Criteria

| Criterion | Status | Notes |
|-----------|--------|-------|
| PostgreSQL 14 image | ✅ YES | Defined in docker-compose |
| Database created | ⏳ BLOCKED | Requires docker-compose up |
| User configured | ✅ YES | Environment vars defined |
| Migrations directory | ✅ YES | Created with .gitkeep |
| node-pg-migrate | ✅ YES | Added to package.json |
| Migration commands | ✅ YES | Scripts defined in package.json |
| Data persistence | ✅ YES | Named volume configured |
| Health check | ✅ YES | Docker healthcheck defined |
| Database connection | ⏳ BLOCKED | Requires running container |
| pg Pool integration | ✅ YES | Code ready in database.provider.ts |

---

## Scope Boundaries (Enforced)

**What IS in baseline:**
- ✅ PostgreSQL connection infrastructure
- ✅ Migration framework (no migrations yet)
- ✅ pg Pool (no ORM)

**What is NOT in baseline (by design):**
- ✗ Business tables (users, vehicles, repairs, etc.)
- ✗ ORM (TypeORM, Sequelize, Prisma forbidden)
- ✗ VS-002 features
- ✗ AI/LLM integration
- ✗ Qdrant/Knowledge/Vision/OCR
- ✗ n8n/Telegram integration

**Validated by:** `scripts/validate-repository.js`

---

## Conclusion

**Status: ⏳ PENDING LOCAL DOCKER EXECUTION**

Migration infrastructure is ready. Database startup and migration execution require local Docker.

**Next Steps (Local):**
1. `docker-compose up -d postgres`
2. `sleep 5 && docker-compose ps` (verify health)
3. `pnpm db:migrate` (apply any pending migrations)
4. `docker exec -it powermech-postgres psql -U powermech -d powermech_dev -c "SELECT version();"`

---

**Evidence Package Location:** `evidence/vertical-slice/vs-001/`

**Previous Document:** [02 — Environment](./02-environment.md)

**Next Document:** [04 — Build, Tests & Runtime](./04-build-tests-runtime.md)
